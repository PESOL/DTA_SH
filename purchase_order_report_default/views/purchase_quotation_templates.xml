<?xml version="1.0" encoding="utf-8"?>
<odoo>
<template id="report_purchasequotation_default_document">
    <t t-call="report.external_layout">
        <t t-set="o" t-value="o.with_context({'lang':o.partner_id.lang})"/>
        <div class="page">
            <div class="oe_structure"/>

            <div class="row mt32 mb32">
                <div class="col-xs-6">
                    <strong>Shipping address:</strong>
                    <div t-if="o.dest_address_id">
                        <div t-field="o.dest_address_id"
                            t-options='{"widget": "contact", "fields": ["address", "name", "phone", "fax"], "no_marker": True, "phone_icons": True}'/>
                    </div>
                    <div t-if="not o.dest_address_id and o.picking_type_id.warehouse_id">
                        <span t-field="o.picking_type_id.warehouse_id.name"/>
                        <div t-field="o.picking_type_id.warehouse_id.partner_id"
                            t-options='{"widget": "contact", "fields": ["address", "phone", "fax"], "no_marker": True, "phone_icons": True}'/>
                    </div>
                </div>
                <div class="col-xs-5 col-xs-offset-1">
                    <div t-field="o.partner_id"
                        t-options='{"widget": "contact", "fields": ["address", "name", "phone", "fax"], "no_marker": True, "phone_icons": True}'/>
                        <p t-if="o.partner_id.vat">VAT: <span t-field="o.partner_id.vat"/></p>
                </div>
            </div>

            <h2>Request for Quotation <span t-field="o.name"/></h2>

            <table class="table table-condensed">
                <thead>
                    <tr>
                      <th><strong>Pos.</strong></th>
                      <th><strong>PROJECT</strong></th>
                      <th class="text-right"><strong>Qty</strong></th>
                      <th><strong>Ref. DTA</strong></th>
                      <th><strong>Description</strong></th>
                      <th><strong>Ref. Supplier</strong></th>
                      <th><strong>Needed date DTA</strong></th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="o.order_line" t-as="order_line">
                        <td>
                            <span t-field="order_line.sequence"/>
                        </td>
                        <td>
                            <span t-field="order_line.account_analytic_id and order_line.account_analytic_id.name"/>
                        </td>
                        <td class="text-right">
                            <span t-field="order_line.product_qty"/>
                            <span t-field="order_line.product_uom" groups="product.group_uom"/>
                        </td>
                        <td>
                            <span t-field="order_line.product_id.default_code"/>
                        </td>
                        <td>
                            <span t-field="order_line.name"/>
                        </td>
                        <td>
                            <span t-field="order_line.product_id.barcode"/>
                        </td>
                        <td class="text-center">
                            <span t-field="order_line.needed_dta_date"/>
                        </td>
                    </tr>
                </tbody>
            </table>

            <p t-field="o.notes"/>

            <div class="oe_structure"/>
        </div>
    </t>
</template>

<template id="report_purchasequotation_default" inherit_id="purchase.report_purchasequotation">
    <xpath expr="t[@t-call='report.html_container']" position="replace">
        <t t-call="report.html_container">
            <t t-foreach="docs" t-as="o">
                <t t-call="purchase_order_report_default.report_purchasequotation_default_document" t-lang="o.partner_id.lang"/>
            </t>
        </t>
    </xpath>
</template>
</odoo>
